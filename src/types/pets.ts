export interface Pet {
    id: string;          
    name: string;        
    description: string; 
    imageUrl: string;
    birthDate: string;
    price: number;
    code: string;        // Código único de la mascota (ej: "DOG123")
  }
  